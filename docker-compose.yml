version: "3.8"
services:
  valhalla:
    image: ghcr.io/valhalla/valhalla:latest
    container_name: valhalla
    ports:
      - "${VALHALLA_PORT:-8080}:8080"
    volumes:
      - ./data:/data
    command: ["valhalla_service", "/data/valhalla.json", "1"]
    restart: unless-stopped
